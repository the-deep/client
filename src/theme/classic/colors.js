const colorNavbar = '#1672c6';
const colorTextOnNavbar = 'rgba(255, 255, 255, .8)';
const colorAccent = '#c51162';
const colorPrimary = '#6d4c41';

const colors = {
    colorNavbar,
    colorTextOnNavbar,
    colorAccent,
    colorPrimary,
};

export default colors;
